const express=require('express');const r=express.Router();const c=require('../controllers/todoController');const auth=require('../middlewares/auth');const authorize=require('../middlewares/authorize');r.use(auth);r.get('/',c.list);r.post('/',c.create);r.get('/:id',c.getById);r.patch('/:id',c.update);r.delete('/:id',authorize('admin'),c.remove);r.patch('/:id/toggle',c.toggle);module.exports=r;